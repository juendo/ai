module.exports = function(data) {
	console.log(moves.map(function(move) {
		return {
			move: move
			player: username,
			turn?,
			winning: true/false,
			game: room,
			number of players
		}
	}));
}